<h1>Editing Book</h1>
<p class="text"><% if @book.errors.any? %>
  <%= @book.errors.count %>error prohibited this book from saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>
</p>

<%= form_with model:@book, local:true do |f| %>

  <p class="text">Title</p>
  <%= f.text_field :title %>


  <p class="text">Body</p>
  <%= f.text_area :body %>

  <p class="text">
  <%= f.submit 'Update Book' %>
  </p>

<% end %>

<p class="text">
<%= link_to "Show", book_path(@book) %>
 | <%= link_to "Back", books_path %>
 </p>